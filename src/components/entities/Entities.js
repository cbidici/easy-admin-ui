import React from "react";
import LayoutContent from "../LayoutContent";
import EntityMenu from "./EntityMenu";

const Entities = props => {
  return (
    <>
      <EntityMenu />
      <LayoutContent>Entities</LayoutContent>
    </>
  );
};

export default Entities;
